## 화면전환 버튼

코드


main
    body: Center(
        child: ElevatedButton(
          child: Text('버튼'),
          onPressed: () {
            Navigator.push(
              context,
              MaterialPageRoute(builder: (context) => NextPage()),
            );
          },
        ),
      ),









      
